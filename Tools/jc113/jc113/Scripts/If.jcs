//JCSTEMPSYS !Remove this comment if you edit the script
function Init()
{
var int MenuItem
Menu.CreateRoot(MenuItem, "New \"if\"", "&Templates")
Menu.Shortcut(MenuItem, "Ctrl+I")
Menu.OnClick(MenuItem, Click)
}
function Click()
{
JassCraft.SelText("\r\nif  then\r\n\r\nendif")
JassCraft.SelStart(JassCraft.SelStart(-1)-14)
}
